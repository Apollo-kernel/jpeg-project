# Images

Place the two required project images here before running `run_tests.py`:

```text
two macaws.png
whitewater rafting.png
```

The scripts preserve these exact names because they match the Stage-1 JPEG-LS notebook flow.
